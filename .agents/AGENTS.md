# Antigravity Agent Rules

- **Git Push Confirmation**: Always ask the user for explicit confirmation before running `git push` commands. When asking, always present a multiple-choice option with "Yes" (Option 1) and "No" (Option 2) so the user can quickly choose with their keyboard.
